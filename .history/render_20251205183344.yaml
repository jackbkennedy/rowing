services:
  - type: web
    plan: starter
    previewPlan: starter
    name: rowing-data-scraper-api
    runtime: node
    buildCommand: yarn install && yarn db:generate && yarn build && npx prisma migrate deploy
    startCommand: yarn start
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rowing_db
          property: connectionString
      - key: PORT
        value: 3000
      # Add any other environment variables your scraper might need
      # - fromGroup: rowing-env

databases:
  - name: rowing-db
    databaseName: rowing_db
    user: rowing_db
    plan: starter
    previewPlan: starter
